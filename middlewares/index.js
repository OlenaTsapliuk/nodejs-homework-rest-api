const validation = require("./validation");
const controllerWrapper = require("./controllerWrapper");
const authenticate = require("./authenticate");
const sizeJimp = require("./sizeJimp");
const upload = require("./upload");

module.exports = {
  validation,
  controllerWrapper,
  authenticate,
  sizeJimp,
  upload,
};
